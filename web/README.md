# Web game MVP

Mở `index.html` bằng một local web server hoặc deploy HTTPS để camera hoạt động.
Pose được xử lý cục bộ bằng MediaPipe trong trình duyệt. Bản này giữ lại prototype
Python ở thư mục gốc để tham chiếu gameplay.
